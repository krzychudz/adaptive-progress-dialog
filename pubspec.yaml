name: adaptive_progress_dialog
description: A Flutter package providing am adaptive progress dialog.
version: 0.0.1
homepage: https://github.com/krzychudz/adaptive-progress-dialog

environment:
  sdk: '>=2.19.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
